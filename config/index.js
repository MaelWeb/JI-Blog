const config = {
    // 启动端口
    port: 8080,

    // 数据库配置
    database: {
        DATABASE: 'node_koa_test',
        USERNAME: 'root',
        PASSWORD: "1123",
        PORT: 3306,
        HOST: 'localhost'
    }
};

module.exports = config;